include <stdio.h>

int main(){
   /*
	* for(i=0;i<lim-1 && (c=getchar()) != '\n' && c!=EOF;++i)
	*   s[i] = c;
	*/

    /*
       for(i=0;i<lim-1;++i){
	      c=getchar();
		  if(c!='\n'){
		    if(c!=EOF){
		       s[i] = c;	
			}
		  }
	   }
	*/  
	return 0;
}
