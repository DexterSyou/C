#include <stdio.h>

int main(int argc, char *argv[]){
      //pBQ.c
	               // 与 char* *argv指向字符指针的指针  等效
                   // 一个指向字符串的指针数组
				   // argv[0] 程序的名字
				   //
				   // 数组名argv指向argv[0]，argv[0]指向字符的指针
      
	  //pBR.c
	  //atoi(argv[1]) --->   
	  //atof()        --->   double
	  //atol()        --->   long
   
      //pBS.c
	  //long strtol(const char *nptr, char **endptr, int base);
}
