##

```
cat conCATenate

cat file1 file2 file3  cat读取文件内容
将命令行参数的文件内容拼接在一起并将结果发送到stdout

cat从标准输入stdin中读取
管道操作符可以将数据作为cat的标准输入

- 代表stdin
echo "demo" | cat - testfile

cat -s file 
cat -T file 
cat -n file
```
